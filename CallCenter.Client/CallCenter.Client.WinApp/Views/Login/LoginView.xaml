<UserControl x:Class="CallCenter.Client.WinApp.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CallCenter.Client.WinApp.Views.Login"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Username"></TextBlock>
            <TextBox Text="{Binding Username}" Width="200"></TextBox>
            <TextBlock Text="Password"></TextBlock>
            <TextBox Text="{Binding Password}" Width="200" ></TextBox>
            <Button cal:Message.Attach="Login()" Content="Login"></Button>
        </StackPanel>
        
    </Grid>
</UserControl>
